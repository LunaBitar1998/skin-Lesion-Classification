torch==2.0.1+cu118           # PyTorch (ensure CUDA version matches the environment)
torchvision==0.15.2+cu118    # Torchvision for pre-trained models
numpy                        # NumPy for numerical operations
Pillow                       # Pillow for image handling
matplotlib                   # Matplotlib for plotting metrics
tqdm                         # Tqdm for progress bars
